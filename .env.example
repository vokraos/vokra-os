# Optional: Wildberries Statistics API token for local dev (read-only).
# Create from WB seller portal → Settings → API access.
# Never commit a real token. Prefer Settings UI localStorage in the app.
VITE_WB_API_TOKEN=
